<%= "Invoice Status: #{@invoice.status}"%><br>
<%= "Invoice Created at: #{@invoice.created_at.strftime("%A, %B%e, %Y")}" %><br>

<%= "Customer Name: #{@invoice.customer.first_name} #{@invoice.customer.last_name}" %><br>

<h3>Total Revenue</h3>
<%= "Total Revenue: #{number_to_currency(@invoice.total_revenue.fdiv(100))}"%>

<h3>Discounted Revenue</h3>
<%= "Discounted Revenue: #{number_to_currency(@invoice.discount_revenue.fdiv(100))}" %>